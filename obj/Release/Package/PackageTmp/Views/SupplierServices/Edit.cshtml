@model RetailKing.Models.SupplierService 

@{
    ViewBag.Title = "Edit";
}

<div class="row">
    <div class="col-md-12">
        <div class="x_panel">
            <div class="x_title">
                <h2>Edit Service </h2>
                <div class="clearfix"></div>
            </div>
            <div class="x_content">
                <div class="row">
                    @using (Html.BeginForm("edit", "SupplierServices", FormMethod.Post, new { id = "editSuppliers" }))
                    {
                        @Html.ValidationSummary(true)
                        <fieldset>
                           
                            @Html.HiddenFor(model => model.Id)
                            @Html.HiddenFor(model => model.SupplierId)
                            @Html.Partial("_CreateOrEdit", Model)

                            <p>

                            </p>
                        </fieldset>
                    }
                    <br/>
                    <div>
                        <button class="btn btn-success" onclick="SaveView('editSuppliers')">Save</button>
                        <button class="btn btn-warning" onclick="GetView('@Url.Action("Index", "Suppliers")')">Cancel</button>
                    </div>
</div>
</div>
</div>
</div>
</div>