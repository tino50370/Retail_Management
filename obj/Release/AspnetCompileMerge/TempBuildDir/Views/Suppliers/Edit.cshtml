@model RetailKing.Models.Supplier

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm("edit", "Suppliers", FormMethod.Post, new { id = "editSuppliers" })){
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Customer</legend>

        @Html.HiddenFor(model => model.ID)
		@Html.Partial("_CreateOrEdit", Model)

        <p>
           
        </p>
    </fieldset>
}
<div>
  <button class="btn btn-black"  onclick="SaveView('editSuppliers')" >Save</button>
  <button class="btn btn-black" onclick="GetView('@Url.Action("Index", "Suppliers")')">Cancel</button>
</div>